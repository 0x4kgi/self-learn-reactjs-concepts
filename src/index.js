import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './app'


//In index, we just render the entire app, the code will be at app.js
ReactDOM.render(<App />, document.getElementById('root'));
